# Example secrets.yaml configuration
# Copy this to /config/secrets.yaml and update with YOUR values

# Sungrow Inverter Modbus Connection
sungrow_modbus_host_ip: 192.168.1.145  # ← Change to YOUR inverter IP address
sungrow_modbus_port: 502               # Leave as 502 (standard Modbus TCP port)
sungrow_modbus_slave: 1                # Leave as 1 (default slave ID)

# How to find your inverter IP:
# 1. Check router DHCP table for "WiNet-S" device
# 2. Connect to WiNet-S WiFi and check http://10.10.100.254
# 3. Use nmap: nmap -p 502 192.168.1.0/24

# Example values for different network setups:
# Standard home network:    192.168.1.145
# Alternative subnet:       192.168.0.145
# Static IP assigned:       10.0.0.145
# Direct connection:        169.254.1.1

# Security Note:
# NEVER commit secrets.yaml to version control!
# Add secrets.yaml to your .gitignore file
